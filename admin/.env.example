# AsmatBet cashier admin — local PrinterBridge (thermal printing)
# Set these in Vercel for production builds of https://admin.asmatbet.com

# Must match printer-service-new/config.json apiKey on each cashier PC
VITE_PRINTER_API_KEY=michotbet-local-print-v1

# Optional: skip auto-probe of localhost:3005–3010
# VITE_PRINT_SERVICE_URL=http://localhost:3005

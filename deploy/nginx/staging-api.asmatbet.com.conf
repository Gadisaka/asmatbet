# Ubuntu + nginx reverse proxy for the staging Docker Compose backend (127.0.0.1:3002).
# Domain: staging-api.asmatbet.com
#
# DNS: A-record staging-api.asmatbet.com → same VPS as api.asmatbet.com
#
# Install on the VPS:
#   sudo cp deploy/nginx/staging-api.asmatbet.com.conf /etc/nginx/sites-available/staging-api.asmatbet.com.conf
#   sudo ln -sf /etc/nginx/sites-available/staging-api.asmatbet.com.conf /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#
# TLS (Let's Encrypt) — after DNS resolves:
#   sudo certbot --nginx -d staging-api.asmatbet.com
#
# Verify:
#   curl -sS https://staging-api.asmatbet.com/health

server {
    listen 80;
    listen [::]:80;
    server_name staging-api.asmatbet.com;

    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location = /nginx-health {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }
}

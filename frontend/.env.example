# Local: leave empty so Vite proxies `/api` to the backend (see vite.config.js).
# Production build: https://api.asmatbet.com
VITE_API_URL=
VITE_GAME_BASE_URL=https://games.asmatbet.com
VITE_MAX_PREMATCH_DAYS=3
# OddsPapi key lives in backend/.env (ODDSPAPI_API_KEY). Do not put it here.
